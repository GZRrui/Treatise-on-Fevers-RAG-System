# 贡献规范

## 分支与范围

- 分支使用 `codex/phase-<阶段>-<短目标>`；一个分支只服务一个阶段和一个可验收目标。
- 禁止把阶段 1.5/2 的实现合并到阶段 0，禁止整体合并 `codex/pre-phase-high-order-snapshot`。
- 开始修改前保持工作区干净；发现他人未提交修改时先隔离，不得强制重置。

## 提交

提交信息使用 `type(scope): summary`，其中 `type` 为 `feat`、`fix`、`test`、`docs`、`refactor`、`build` 或 `chore`。每个提交必须能独立解释、测试和回滚，不提交密钥、缓存、虚拟环境、构建输出或运行时数据。

依赖变更必须同时更新直接依赖清单和锁文件：Python 使用生成哈希的 `requirements*.txt`，前端使用 `package-lock.json` 和 `npm ci`。禁止手工编辑传递依赖版本。

## Pull Request

PR 必须包含目标阶段、行为影响、测试证据、迁移步骤和回滚步骤。所有适用 CI 门禁必须通过；已知缺陷必须登记目标阶段，不得用宽泛异常捕获、忽略漏洞或降低质量阈值绕过。

## 发布

采用语义化版本 `MAJOR.MINOR.PATCH` 和带签名或受保护的 `vX.Y.Z` 标签。发布候选必须来自受保护分支，附带变更日志、SBOM、漏洞扫描、配置迁移和回滚证据。破坏 API、数据 Schema 或配置契约时提升主版本；兼容功能提升次版本；兼容修复提升补丁版本。
